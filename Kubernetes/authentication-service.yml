apiVersion: v1
kind: Service
metadata:
  name: bikerride-authentication-proxy
spec:
  type: ClusterIP
  ports:
  - name: lagom
    port: 9000
    protocol: TCP
  selector:
    app: authentication-lagom
