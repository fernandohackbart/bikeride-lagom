apiVersion: v1
kind: Service
metadata:
  labels:
    app: bikeride-ride-lagom
  name: bikeride-ride-lagom
spec:
  clusterIP: None
  ports:
    - name: lagom
      port: 9000
      protocol: TCP
  selector:
    app: ride-lagom-impl
---
apiVersion: v1
kind: Service
metadata:
  labels:
    app: bikeride-ride-akka-remoting
  name: bikeride-ride-akka-remoting
spec:
  ports:
    - name: akka-remote
      port: 2551
      protocol: TCP
  selector:
    app: ride-lagom-impl
